[pytest]
testpaths = tests
markers =
    integration: requires a running service at SERVICE_URL (env default http://localhost:18000)
    load: performance / load tests — run with -m load (slow, opt-in)
addopts = -v --tb=short
python_files = test_*.py
python_classes = Test*
python_functions = test_*
